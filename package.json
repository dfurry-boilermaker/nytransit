{
  "name": "nytransit",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "3D interactive map of NYC transit — subways at their real depths, buses on the surface, viewed from the harbor.",
  "scripts": {
    "data": "node scripts/build-data.mjs",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "pngjs": "^7.0.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "three": "^0.169.0"
  }
}
